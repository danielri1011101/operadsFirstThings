\usepackage{listings}
\usepackage{xcolor} % For syntax highlighting colors

\lstdefinelanguage{mylanguage}{%
    % Keywords
    morekeywords={let, if, else, while, import, export},%
    keywordstyle=\color{violet},
    
    % Operators
    morekeywords=[2]{ola, ke, ase},
    keywordstyle=[2]\color{orange},
    
    % Standard strings
    morestring=[b]",
    morestring=[b]',
    stringstyle=\color{green!60!black},
    
    % .operator. strings
    morestring=[s]{.}{.},
    stringstyle=\color{orange!80!black},
    
    % ~;import;~ strings
    morestring=[s]{~;}{;~},
    stringstyle=\color{blue!70!black},
    
    % General formatting
    basicstyle=\ttfamily\small,
    breaklines=true,
    tabsize=2
}
